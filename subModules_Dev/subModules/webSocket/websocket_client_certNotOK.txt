#include "websocket_client.h"

#include "esp_websocket_client.h"
#include "esp_log.h"
#include "time.h"
#include <string.h>
#include "esp_crt_bundle.h"

#include "esp_heap_caps.h"

static const char *TAG = "WEBSOCKET";

static esp_websocket_client_handle_t client = NULL;

const char* ngrok_root_ca = 
"-----BEGIN CERTIFICATE-----\n"
"MIIFCzCCAvOgAwIBAgIRAI09966gHw5SIdn/vIn4e10wDQYJKoZIhvcNAQELBQAw\n"
"TzELMAkGA1UEBhMCVVMxKTAnBgNVBAoTIUlud稼cm5ldCBTZWN1cml0eSBSZXNl\n"
"YXJjaCBHcm91cDEVMBMGA1UEAxMMSVNSRyBSb290IFgxMB4XDTI0MDMyNDAwMDAw\n"
"MFoXDTY0MDMyMzIzNTk1OVowTzELMAkGA1UEBhMCVVMxKTAnBgNVBAoTIUludGVy\n"
"bmV0IFNlY3VyaXR5IFJlc2VhcmNoIEdyb3VwMRUwEwYDVQQDEwxJU1JHIFJvb3Qg\n"
"WDEwggIiMADQYJKoZIhvcNAQEBBQADggIPADCCAgoCggIBAK3oJHgEx2Lt06cWZ\n"
"nyGUxmFnHTPlaG88ab897KlHfpzRFQgC6mX6H97a6qiVAZO1FvReBBA6f0670wW\n"
"KIDBAtwiqUyb9IZdACLXwAnXF84pJxHU46AX80Zm5b4DKuLfG6RMfN7C2x34mFr\n"
"66YNjgmrIDdYiKJEUNGAcBIH7ceeEOng9wGJCTCpnstOJrBVGOXoO0BfAUMlaT6\n"
"S4wI7vesE0rKIKBTLvfkOQcn55jBV6mZIKWLUrPDn5zGs4u9TWmAnkvKRmS5Oic\n"
"2IMANQAZgYJS7jC266dErvMpt6G58KET5srl98xbun7w3nIFsp/Ch8WTCisXyRa\n"
"dCW6fVBlfkaScNYv6TuYejUMztqnLIrGRFiAKXLNAnlamfUhDFB86L8rM4E8OG3\n"
"OQ6576QRqLNAd5hM7eBE4w657veIn04fT4BKC6YFCDyGjKPbaUnNPn85X1EXX69\n"
"pXLY7mwtX2943OfAOZSKCHYgkbv04NMC5U7B4m8qQUXg7K1sFf4kH6rYWtI4gL3\n"
"Rro6P49YAs+A3S9eUBpCo77S3E98MGGWlsVWhfT9vE2AMVAS6bW1wzZgXWWDTBx\n"
"Nokg8m9oJI+f06fGE8H585+IdK7O76vNo7O7H9LXNPh864/EwEAEAAD//wQBAwEA\n"
"AAMBAA==\n"
"-----END CERTIFICATE-----\n"
"-----BEGIN CERTIFICATE-----\n"
"MIICGTCCAZ6gAwIBAgIRAIOfXoBFF/i4v76A6R4rP0EwDAYIKoZIzj0EAwIFADBP\n"
"MQswCQYDVQQGEwJVUzEpMCcGA1UEChMgSW50ZXJuZXQgU2VjdXJpdHkgUmVzZWFy\n"
"Y2ggR3JvdXAxFTATBgNVBAMTDElTUkcgUm9vdCBYMTAeFw0yNDA0MTEwMDAwMDBa\n"
"Fw0yNzA0MTEwMDAwMDBaMDIxCzAJBgNVBAYTAlVTMRkwFwYDVQQKExBMZXQncyBF\n"
"bmNyeXB0MQwwCgYDVQQDEwNZRTEwWTATBgcqhkjOPQIBBggqhkjOPQMBBwNCAAS8\n"
"f2M/D9K/T3U/fT8AWhK9YnF3uXvH8Yw2S5mdfGZ8vAdfS8wZg7fX8Z7vD8wS8fA\n"
"f7vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAf7vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAf\n"
"7vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAf7vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAf7\n"
"vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAdfS8wZg7fX8Z7vD8wS8fAf7vD8w==\n"
"-----END CERTIFICATE-----";


static void websocket_event_handler(void *handler_args, esp_event_base_t base,int32_t event_id,void *event_data)
{

    ESP_LOGI( TAG, "WebSocket event id = %ld", event_id );
    switch(event_id)
    {
    case WEBSOCKET_EVENT_CONNECTED:
        ws_connected = true;
        ESP_LOGI( TAG, "CONNECTED"  );
        break;

    case WEBSOCKET_EVENT_DISCONNECTED:
        ws_connected = false;
        ESP_LOGW( TAG, "DISCONNECTED" );
        break;

    case WEBSOCKET_EVENT_ERROR:
        ESP_LOGE(TAG,"ERROR" );
        break;

    case WEBSOCKET_EVENT_DATA:
    {
        esp_websocket_event_data_t *data =  (esp_websocket_event_data_t *)event_data;
        ESP_LOGI(TAG, "RX data len=%d", data->data_len);
        break;
    }

    default:
        ESP_LOGI(TAG, "OTHER EVENT" );
        break;
    }
}

esp_err_t websocket_client_init( const char *url)
{
    if (url == NULL)
    {
        return ESP_ERR_INVALID_ARG;
    }

    //extern const char ngrok_root_ca_start[] asm("_binary_ngrok_ca_pem_start");
	//extern const char ngrok_root_ca_end[]   asm("_binary_ngrok_ca_pem_end");

	esp_log_level_set( "websocket_client", ESP_LOG_VERBOSE);
	esp_log_level_set( "esp-tls", ESP_LOG_VERBOSE);
	esp_log_level_set( "mbedtls", ESP_LOG_VERBOSE);
	esp_log_level_set( "transport_ws", ESP_LOG_VERBOSE);
    esp_log_level_set( "transport_base", ESP_LOG_VERBOSE);
	
	/*
     * Read synchronized POSIX system time
     */
    time_t now =  time(NULL);

    ESP_LOGI( "TIME", "WebSocket module Unix time: %lld",  (long long)now );

	esp_websocket_client_config_t websocket_cfg =
	{
	    .uri = url,
	   // .cert_pem = ngrok_root_ca_start,	
		.crt_bundle_attach = esp_crt_bundle_attach,
        .reconnect_timeout_ms = 5000,
        .network_timeout_ms = 10000,
        .user_agent = "ESP32S3-Client",
        .task_stack = 10240,  // Increase internal task stack space to 10KB
        .buffer_size = 2048,  // Keep communication frame buffers tightly scaled
	    //.subprotocol = "chat",
	    .skip_cert_common_name_check = false,
	};

    
    ESP_LOGI( TAG, "Largest internal block before WebSocket init: %lu", (unsigned long) heap_caps_get_largest_free_block( MALLOC_CAP_INTERNAL));
    ESP_LOGI( TAG,  "Free heap before WebSocket init: %lu",(unsigned long)esp_get_free_heap_size());
    
    client = esp_websocket_client_init( &websocket_cfg );
    if(client == NULL)
    {
        return ESP_FAIL;
    }

    esp_websocket_register_events( client, WEBSOCKET_EVENT_ANY, websocket_event_handler, NULL );
	
    esp_err_t ret =  esp_websocket_client_start( client);

    if(ret == ESP_OK)
    {
        ESP_LOGI( TAG,  "WebSocket started"  );
    }


    return ret;
}

bool websocket_client_is_connected(void)
{
    if (client == NULL)
    {
        return false;
    }

    return esp_websocket_client_is_connected(client);
}

esp_err_t websocket_send_text(const char *message)
{

    ESP_LOGI(TAG, "websocket_send_text() called");


    if(client == NULL)
    {
        ESP_LOGE(TAG, "Client handle is NULL");
        return ESP_ERR_INVALID_STATE;
    }


    ESP_LOGI(TAG, "Client handle OK: %p", client);


    ESP_LOGI( TAG, "ws_connected flag = %d",   ws_connected);


    bool connected = esp_websocket_client_is_connected(client);


    ESP_LOGI(TAG, "ESP websocket connected state = %d", connected);


    if (!connected) { ESP_LOGW(TAG, "Websocket not connected yet"); return ESP_ERR_INVALID_STATE; }


    int len = strlen(message);
    ESP_LOGI(TAG, "Sending %d bytes: %s", len, message);
    int sent = esp_websocket_client_send_text(client, message, len, portMAX_DELAY);


    if (sent < 0) { ESP_LOGE(TAG, "esp_websocket_client_send_text failed"); return ESP_FAIL; }
    ESP_LOGI(TAG, "Successfully sent %d bytes", sent);
    return ESP_OK;
}

void websocket_client_stop(void)
{
    if(client)
    {

        esp_websocket_client_stop(client);


        esp_websocket_client_destroy(client);


        client = NULL;
    }
}
